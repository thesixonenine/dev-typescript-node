{
  "name": "thesixonenine/dev/typescript-node:24-trixie",
  "build": {
    "dockerfile": "Dockerfile",
    "args": { "GITHUB_PAT": "${localEnv:GITHUB_PAT}" }
  },
  "customizations": { "vscode": { "settings": {
        "extensions.allowed": {
            "microsoft": false,
            "github": false
        }
  }}}
}
