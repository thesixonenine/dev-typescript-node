{
  "name": "dev-typescript-node",
  "build": {
    "dockerfile": "Dockerfile",
    "args": { "GITHUB_PAT": "${localEnv:GITHUB_PAT}" }
  },
  "postStartCommand": "chezmoi update",
  "customizations": { "vscode": 
    { 
      "extensions": ["-dbaeumer.vscode-eslint"],
      "settings": {
        "extensions.allowed": {
            "microsoft": false,
            "github": false
        }
  }}}
}
